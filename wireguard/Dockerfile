FROM linuxserver/wireguard

ARG PUID
ARG PGID
ARG TZ
ARG SERVERPORT
ARG PEERS
ARG PEERDNS


ENV PUID=${PUID}
ENV PGID=${PGID}
ENV TZ=${TZ}
ENV SERVERPORT=${SERVERPORT}
ENV PEERS=${PEERS}
ENV PEERDNS=${PEERDNS}